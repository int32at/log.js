(function() {
  describe('logger.coffee specs', function() {
    return it('should be possible to use the logger object', function() {
      return expect(logger).not.toBeUndefined();
    });
  });

}).call(this);
